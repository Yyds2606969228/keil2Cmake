## 1. 模块拆分与解耦 [x]
### 进入条件
- 已确认 `openocd_mcp` 需要独立为同仓库子项目。

### 子任务
1.1 迁移并收敛到 `src/openocd_mcp/src/openocd_mcp` [x]  
1.2 迁移并收敛到 `src/openocd_mcp/tests/openocd_mcp` [x]  
1.3 迁移 `docs/debug_runtime` 与 `skills/*` 到 `src/openocd_mcp` 子项目 [x]  
1.4 新增 `src/openocd_mcp` 独立入口/依赖/spec/README [x]  
1.5 keil2cmake 移除 `mcp-debug` 运行时耦合 [x]

### 验证
- `rg` 检查 `src/keil2cmake` 内无 `openocd_mcp` / `mcp-debug` 运行时依赖。

### 产出物
- `src/openocd_mcp/` 子项目目录与独立运行文件。

## 2. CMake->Keil 同步功能 [x]
### 进入条件
- 已确认同步策略：原地更新+`.bak` 备份，只维护同步组。

### 子任务
2.1 新增 `src/keil2cmake/keil/sync.py`，解析 `keil2cmake_user.cmake` [x]  
2.2 实现 `.uvprojx` 写回（sources/include/define/misc flags） [x]  
2.3 实现 `K2C_Sync` 组维护与 `--prune` 逻辑 [x]  
2.4 实现 `--dry-run` 与备份逻辑 [x]  
2.5 接入 CLI 子命令 `sync-keil` [x]

### 验证
- 新增单测覆盖同步路径、组策略、target 选择、dry-run。

### 产出物
- `sync-keil` CLI 可用。

## 3. 配置/打包/文档更新 [x]
### 进入条件
- 功能代码与目录迁移完成。

### 子任务
3.1 更新 `Keil2Cmake.spec` 与 `requirements.txt` 依赖边界 [x]  
3.2 更新 `tests/test_packaging_spec.py` [x]  
3.3 更新 `README.md` 与 `README_EN.md` 的命令和模块边界 [x]

### 验证
- `tests.test_packaging_spec` 通过。

### 产出物
- keil2cmake 与 openocd_mcp 依赖边界清晰。

## 4. 测试与收尾 [x]
### 进入条件
- 所有改动已落地。

### 子任务
4.1 运行 keil2cmake 相关单测 [x]  
4.2 运行 openocd_mcp 子项目 pytest [x]  
4.3 记录发现并沉淀 memory [x]

### 验证
- 关键测试命令全部通过。

### 产出物
- 可回归验证记录与 memory 更新。
